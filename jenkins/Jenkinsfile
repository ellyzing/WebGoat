pipeline {
   agent {
     node {
      label 'TestNode' 
   }
}
stages {
    
    stage('Checkout') {
    steps {
    	echo 'step Git Checkout'
		checkout scm
        }
    }
    stage('Build') {
        steps {
        ls
        cd WebGoat
        sh 'mvnw clean install' 
        sh 'docker build -f Dockerfile . -t webgoat/webgoat'
        }
    }

}
}
